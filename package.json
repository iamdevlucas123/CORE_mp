{
  "name": "core-mp",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @core/web run dev",
    "dev:api": "npm --workspace @core/api run dev",
    "db:migrate": "npm --workspace @core/api run migrate",
    "build": "npm -ws run build"
  }
}
